import tomJerryNibbles.*

describe "Grupo de test" {

  test "la Energia de Tom es igual a 50 de entrada" {
    assert.equals(50, tom.energia())
  }

  test "Puede recorrer cierta distancia = 30" {
    assert.that(tom.puedeCazar(30))
  }

  test "el peso de jerry es 40" {
    assert.that(jerry.peso())    
  }

}